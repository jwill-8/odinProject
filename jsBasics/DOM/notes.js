// document.querySelector gets the first instance of a node that matches selector given
// selector can be any css selctor

// document.querySelctorAll get's all nodes that match a selector

// document.CreateElement creates an el

// parent.appendChild(childNode) adds to parent as last child node

// parent.insertBefore(newChild, referenceChild) inserts a new node nefore a given node

// parentNode.removeChild(child) // removes

// document.createElement(el) creates an element

// can alter properties of the created el through dot notation or []

// kebab case CSS properties need to use [] to use kebab case or the camelCase variation for .

// el.setAttribute allows you to set several sttributes at once

// getAttribute gets

// removeAttribute removes 

// can add or remove class with add.remove methods

// textContent method prefered way to add text

// JS does NOT alter HTML file, rather changes what the browser renders



